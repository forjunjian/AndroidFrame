<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.forjun.testdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".app.TestDemoAppclication"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".DemoHucActivity">

        </activity>
        <activity android:name=".base.adapter.FootRecycleViewActivity">
        </activity>
        <activity android:name=".base.ui.activity.ShowKKTestActivity"/>
        <!--<activity android:name=".base.adapter.ui.activity."/>-->
    </application>

</manifest>